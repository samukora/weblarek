# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные 

#### Интерфейс IProduct
Интерфейс описывает данные о товаре.

Свойства:
| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| id | string | уникальный идентификатор |
| title | string | наименование |
| image | string | путь к файлу изображения |
| category | string | категория |
| price | number|null | стоимость |
| description | string | описание |

#### Интерфейс ICustomer
Интерфейс описывает данные о покупателе.

Свойства:
| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| payment | TPayment | тип оплаты: наличный расчет/банковская карта |
| address | string | адрес для доставки |
| email | string | адрес электронной почты |
| phone | string | номер телефона |


#### Интерфейс IOrder 
Интерфейс описывает модель данных для отправки данных о заказе на сервер.

Свойства:
| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| payment | TPayment | тип оплаты: наличный расчет/банковская карта |
| address | string | адрес для доставки |
| email | string | адрес электронной почты |
| phone | string | номер телефона |
| total | number| сумма заказа |
| items | string[]| массив идентификаторов продуктов |

#### Интерфейс IOrderResponse 
Интерфейс описывает модель данных для получения данных о успешно зарегистрированном заказе.

Свойства:
| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| id | string | номер заказа |
| total | number | сумма заказа |

#### Интерфейс IProductResponse 
Интерфейс описывает модель данных для получения данных о каталоге товаров

Свойства:
| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| total | number | кол-во товаров в списке |
| items | IProduct[] | список товаров |

#### Интерфейс ICard 
Интерфейс описывает базовую модель данных для карточки товара.

| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| titleElement | HTMLElement | наименование товара |
| priceElement | HTMLElement | цена товара |

#### Интерфейс IActions
Интерфейс описывает базовые функции для обработчиков событий

| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| onClick | function | функция для базового события |

#### Интерфейс IForm
Интерфейс описывает базовые элементы для форм

| Параметр | Тип | Описание |
| :--- | :--- | :--- |
| submitButtonElement | HTMLButtonElement | кнопка подтверждения |
| errorsElement | HTMLElement | блок ошибок |

### Модели данных

#### Класс Catalog  
Класс предназначен для хранения и обработки списка существующих товаров.  

Конструктор:  
`constructor()` - конструктор принимает объект с типом IEvents и сохраняет его в классе.  

Поля класса:  
`private listOfProducts: IProduct[]` - массив товаров.  
`private currentProduct: IProduct | null` - данные по выбранному товару.  
`private eventEmitter: IEvents` - обьект для работы с событиями.  

Методы:  
`getList(): IProduct[]` - метод отвечающий за получение полного перечня товаров доступных для оторажения. Метод не имеет параметров, возвращает массив данных с типом IProduct.  
`getCurrentProductDetails(): IProduct | null` - метод для получения данных о выбранном товаре, возвращает обьект типа IProduct.  
`getItem(id: string):IProduct | null` - метод для получения информации о товаре. На вход принимает уникальный идентификатор товара, возвращает обьект с типом IProduct.  
`setCurrentProduct(id: string): void` - метод для сохранения данных о выбранном продукте. При вызове принимает уникальный идентификатор продукта. Вызывает событие "card:preview".  
`setList(list: IProduct[]): void`- метод для сохранения нового списка товаров. На вход принимает массив объектов с типом IProduct. При использовании данного метода перечень товаров перезаписывается новыми данными. Вызывает событие "catalog:change".  

#### Класс Basket  
Класс предназначен для хранения и обработки информации о корзине покупателя со списком выбранных товаров.  

Конструктор:  
`constructor()` - конструктор принимает объект с типом IEvents и сохраняет его в классе.  

Поля класса:  
`private listOfProducts: IProduct[]` - массив выбранных товаров.  
`private eventEmitter: IEvents` - обьект для работы с событиями.  

Методы:  
`addItem(item: IProduct): void` -  метод позволяет добавить продукт в корзину покупок. На вход передается карточка товара. Вызывается событие "basket:change".  
`removeItem(item: IProduct): void` -  метод позволяет удалять продукт из корзины покупок. На вход передается карточка товара. Вызывается событие "basket:change".  
`getTotalCount(): number` - метод возвращает количество товаров в корзине.  
`getList(): IProduct[]` - метод возвращает список товаров в корзине покупок, возвращает массив с типом элеметов IProduct.  
`getTotalAmount(): number`- метод возвращает итоговую сумму для оплаты.  
`checkAvailability(id: string): boolean` - метод проверяет наличие товара в корзине и возвращает результат доступности функционала добавления данного товара в корину: true/false.  
`clearBasket(): void` -  метод очищает корзину для покупок. Вызывается событие "basket:change".  

#### Класс Customer 
Класс предназначен для хранения и обработки информации о покупателе.  

Конструктор:  
`constructor()` - конструктор принимает объект с типом IEvents и сохраняет его в классе.  

Поля класса:  
`payment: TPayment` - тип оплаты: банковская карта(card) или наличный рассчет(cash).  
`address: string` - адрес доставки заказа.  
`email: string` - адрес электронной почты.  
`phone: string` - номер телефона для связи.  
`private eventEmitter: IEvents` - обьект для работы с событиями.  

Методы:  
`setInfo(customer: ICustomer): void` - сохранение данных покупателя, на вход подается объект с типом ICustomer. Вызывает события "order:change"/"contacts:change" в соответствии с изменяемым значением.  
`getInfo(): ICustomer` - метод возвращает данные о покупателе, возвращает обьект с типом ICustomer.  
`clearInfo(): void` - метод очищает информацию о покупателе.  
`validateInfo(): TCustomerErrors` - метод позволяет валидировать данные получателя на корректность заполнения, возвращает объект с результатом проверки и перечнем полей, в которых найдены ошибки валидации.  

### Слой коммуникации
#### Класс StoreService
Класс предназначен для интеграции с сервером.  

Конструктор:  
`constructor(api: IApi)` - конструктор класса принимает обьект IApi и сохраняет его как поле apiService.  

Поля:  
`apiService` - поле принимающее обьект класса Api, для последующего взаимодействия с сервером.  

Методы:  
`async  getData(): Promise<IProduct[]>` - метод делает get запрос на эндпоинт /product/ и возвращает массив товаров.  
`async postOrder(data: IStore): Promise<IStore>` - метод делает post запрос на эндпоинт /order/ и передаёт в него данные, полученные в параметрах метода.  

### Слой представления

#### Класс Header  
Класс для отображения шапки сайта. Родительский класс - Component<IHeader>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IActions для обработки событий. Также находит внутри переданного компонента елементы с классами "header__basket-counter" и "header__basket", сохраняет их в классе, после чего вешает на последний базовое событие из actions.  

Поля:  
`public counter: number` - кол-во элементов в корзине.  
`private counterElement: HTMLElement` - счетчик элементов в корзине.  
`private basketElement: HTMLElement` - кнопка корзины.  

Методы:  
`set counter(value: number)` - сеттер для кол-ва элементов в корзине.  

#### Класс Card  
Абстрактный класс для карточки товара, дженерик, родительский класс - класс Component.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super(). А также находит внутри переданного компонента елементы с классами "card__title" и "card__price" и сохраняет их в классе.  

Поля:  
`protected titleElement: HTMLElement` - наименование товара.  
`protected priceElement: HTMLElement` - стоимость товара.  

Методы:  
`set title(value: string)` - сеттер для наименования товара.  
`set price(value: number)` - сеттер для стоимости товара.  


#### Класс CardCatalog
Класс для работы с карточкой товара в каталоге, родительский класс - класс Card<ICardCatalog>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа ICardActions для обработки событий. Также находит внутри переданного компонента елементы с классами "card__image" и "card__category", сохраняет его в классе и вешает базовое событие из actions.  


Поля:  
`protected categoryElement: HTMLElement` - категория товара.  
`protected imageElement: HTMLElement` - изображение товара.  

Методы:  
`set category(value: string)` - сеттер для категории товара.  
`set image(value: string)` - сеттер для изображения товара.  

#### Класс CardPreview  
Класс для работы с карточкой товара в режиме просмотра информации, родительский класс - класс Card<ICardPreview>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа ICardActions для обработки событий. Также находит внутри переданного компонента елементы с классами "card__image", "card__category", "card__text", "card__button", сохраняет их в классе и вешает на последний базовое событие из actions.  

Поля:  
`protected imageElement: HTMLElement` - изображение товара.  
`protected categoryElement: HTMLElement` - категория товара.  
`protected descriptionElement: HTMLElement` - описание товара.  
`protected actionButtonElement: HTMLButtonElement` - кнопка для добавления/удаления товара из корзины покупок.  

Методы:  
`set image(value: string)` - сеттер для изображения товара.  
`set category(value: string)` - сеттер для категории товара.  
`set description(value: string)` - сеттер для описания товара.  
`set buttonDisable(value: boolean)` - сеттер для недоступности кнопки добавления/удаления товара из корзины покупок.  
`set buttonText(value: string)` - сеттер для изменения надписи на кнопке добавления/удаления товара из корзины покупок.  

#### Класс CardBasket  
Класс для работы с карточкой товара в корзине, родительский класс - класс Card<ICard>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IActions для обработки событий. Также находит внутри переданного компонента елементы с классами "basket__item-index" и "card__button", сохраняет их в классе и вешает на последний базовое событие из actions.  

Поля:  
`private actionButtonElement: HTMLButtonElement;` - кнопка для удаления товара из корзины покупок.  
`private indexElement: HTMLElement` - порядковый номер элемента в списке товаров.  

Методы:  
`set index(value: number)` - сеттер для порядкового номера.

#### Класс Form
Абстрактный класс для описания формы, дженерик, родительский класс - класс Component.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IActions для обработки событий. Также находит внутри переданного компонента елементы по селекторам "form__errors" и "button[type=submit]", сохраняет их в классе и вешает на последний базовое событие из actions.  

Поля:  
`protected submitButtonElement: HTMLButtonElement` - кнопка для продолжения обработки формы.  
`protected errorsElement: HTMLElement` - блок ошибок.  

Методы:  
`set canContinue(value: boolean)` - сеттер для определения доступности кнопки.  
`set errors(values: {})` - сеттер для ошибок валидации.  


#### Класс FormOrder  
Класс для работы с формой заказа, родительский класс - класс Form<IFormOrder>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IActions для обработки событий. Также находит внутри переданного компонента елементы по селекторам "order__buttons", "input[name='address']","button[name='card']", "button[name='cash']", сохраняет их в классе и вешает соответствующие обработчики из actions.  

Поля:  
`private paymentButtonsElement: HTMLElement` - блок с элементами способов оплаты.  
`private cardButtonElement: HTMLButtonElement` - способ оплаты по карте.  
`private cashButtonElement: HTMLButtonElement` - способ оплаты за наличный расчет.  
`private addressElement: HTMLElement` - адрес доcтавки.  

Методы:  
`set payment(value: TPayment)` - сеттер для установки способа оплаты.  
`set address(value: string)` - сеттер для адреса доставки.  

#### Класс FormContacts. 
Класс для работы с формой контактов, родительский класс - класс Form<IFormContacts>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IEvents для обработки событий. Также находит внутри переданного компонента елементы по селекторам "input[name='email']" и "input[name='phone']", сохраняет их в классе и вешает соответствующие события.  

Поля:  
`private emailElement: HTMLElement` - адрес электронной почты покупателя.  
`private phoneElement: HTMLElement` - контактный номер покупателя.  

Методы:  
`set email(value: string)` - сеттер для адреса электронной почты.  
`set phone(value: string)` - сеттер для контактного номера.  

#### Класс Modal
Класс для работы с модальным окном. Родительский класс - Component<IModal>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super(). Также находит внутри переданного компонента елементы с классами ".modal__content" и ".modal__close", сохраняет их в классе и вешает соответствующие события.  

Поля:  
`private modalContentElement: HTMLElement` - элемент для отображения контента.  
`private closeElement: HTMLElement` - кнопка закрытия модального окна.  

Методы:  
`set content(child: HTMLElement)` - сеттер для обновления контента модального окна.  
`open()` - метод для открытия модального окна.  
`close()` - метод для закрытия модального окна.  

#### Класс BasketView  
Класс для работы с корзиной товаров. Родительский класс - Component<IBasket>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IActions для обработки событий. Также находит внутри переданного компонента елементы с классами "basket__list", "basket__price" и "basket__button", сохраняет их в классе и вешает на последний базовое событие из actions.  

Поля:  
`private basketListElements: HTMLElement` - список товаров в корзине.  
`private basketTotalAmountElement: HTMLElement` - общая сумма заказа.  
`private basketButtonElement: HTMLButtonElement` - кнопка оформления заказа.  

Методы:  
`set basket(items: HTMLElement[])` - сеттер для списка корзины.  
`set totalAmount(value: number)` - сеттер для общей суммы товаров.  
`set buttonDisable(value: boolean)` - сеттер для доступности кнопки оформления заказа.  

#### Класс Gallery  
Класс для работы с галереей товаров. Родительский класс - Component<IGallery>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super().  

Поля:  
`galleryList: HTMLElement[]` - список товаров.  

Методы:  
 `set galleryList(items: HTMLElement[])` - сеттер для контента елемента.  

#### Класс Success  
Класс для работы с формой оформленного заказа. Родительский класс - Component<ISuccess>.  

Конструктор: принимает обьект HTMLElement, который передает в родительский конструтор super() и обьект типа IActions для обработки событий. Также находит внутри переданного компонента елементы с классами "order-success__description", "order-success__close",  сохраняет их в классе и вешает на последний базовое событие из actions.  

Поля:  
`private totalAmountElement: HTMLElement` - сумма заказа.  
`private closeButtonElement: HTMLButtonElement` - кнопка перехода к другим покупкам.  

Методы:  
`set totalAmount(value: number) ` - сеттер для суммы заказа.  
 
### События  

`catalog:change` - действие срабатывает при изменении состава каталога.  
`card:select` - действие срабатывает при выборе карточки товара из галереи.  
`card:preview` - действие срабатывает при сеттинге данных по выбранной карточке.  
`card:remove` - действие срабатывает при удалении товара из корзины.  
`card:buy` - действие срабатывает при добавлении товара в карзину.  
`basket:open` - действие срабатывает при нажатии на значок корзины.  
`basket:change` - действие срабатывает при изменении состава корзины покупок.  
`customer:change` - действие срабатывает при изменении данных покупателя.  
`order:change` - действие срабатывает при изменении данных по заказу.  
`contacts:change` - действие срабатывает при изменении контактной информации.  
`basket:order` - действие срабатывает при нажатии кнопки оформления заказа в корзине.  
`order:submit` - дейсвие срабатывает при нажатии кнопки продолжить в карточке заполнения данных по заказу.  
`contacts:submit` - действие срабатывает при нажатии кнопки оформления заказа в карточке ввода контакной информации.  
`success:finish` - действие срабатывает при нажатии на кнопку "вернуть к покупкам" на карточке с отбивкой об успешном оформлении заказа.  